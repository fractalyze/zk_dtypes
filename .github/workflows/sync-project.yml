name: Sync PR to Project

on:
  pull_request_target:
    types: [opened, reopened, converted_to_draft, ready_for_review, closed]

jobs:
  sync:
    uses: fractalyze/.github-private/.github/workflows/sync-project-fields.yml@main
    secrets:
      PROJECT_TRACKER_GH_PAT: ${{ secrets.PROJECT_TRACKER_GH_PAT }}
